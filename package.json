{
  "name": "gfa",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "lint": "prettier . --check",
    "lint:fix": "prettier . --write",
    "dev": "ts-node ./index",
    "test": "ts-node ./test",
    "clean": "rm -rf ./build && rm -f ./gfa",
    "dist": "run-s build deploy",
    "deploy": "cp gfa $HOME/bin/",
    "build": "run-s build:bundle build:shebang build:executable",
    "build:bundle": "esbuild index.ts --bundle --platform=node --target=node20.9 --minify --tree-shaking=true --outdir=./build",
    "build:shebang": "node -e \"const fs = require('fs'); fs.writeFileSync('./gfa', fs.readFileSync('./prolog.txt', 'utf-8') + fs.readFileSync('./build/index.js', 'utf-8'));\"",
    "build:executable": "node -e \"const fs = require('fs'); fs.chmodSync('./gfa', 0o755);\""
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "esbuild": "0.21.5",
    "npm-run-all": "^4.1.5",
    "prettier": "3.3.2",
    "ts-node": "^10.9.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ansi-escapes": "^7.0.0",
    "ansi-styles": "^6.2.1"
  }
}
