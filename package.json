{
  "name": "gfa",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "bundle": "bun build ./index.ts --outdir ./build --target node --minify",
    "clean": "rm -rf ./build",
    "dev": "bun run ./index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ansi-escape-sequences": "^6.2.2"
  }
}